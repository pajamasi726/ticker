spring:
  application:
    name: ticker-server
  threads:
    virtual:
      enabled: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health   # explicit; never env/configprops

ticker:
  server:
    enabled: true
