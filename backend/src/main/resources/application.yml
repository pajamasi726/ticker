spring:
  application:
    name: ticker
  threads:
    virtual:
      enabled: true

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health   # explicit; never env/configprops
